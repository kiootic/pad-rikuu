export interface EnemySkill {
  id: number;
  name: string;
  type: number;
  params: [
    string, number, number, number,
    number, number, number, number,
    number, number, number, number,
    number, number, number, number
  ];
}